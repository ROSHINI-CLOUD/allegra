// Every path (/, /discover, /library, /liked, /artist/[name], /album, /playlist/[id],
// /shared/[code], /settings) is rendered by the persistent shell in app/layout.tsx.
export default function Page() {
  return null;
}
