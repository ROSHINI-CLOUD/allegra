.tools/
node_modules/
dist/
.next/
next-env.d.ts
.env
.env.*
!.env.example
.playwright-cli/
output/playwright/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
.gstack/

.vercel/
.claude/

# Large ONNX models for browser live karaoke (download locally; do not commit)
apps/web/public/models/*.onnx
apps/web/public/models/*.ort
