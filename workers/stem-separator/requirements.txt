# Pin production deps. Torch CUDA wheels are installed separately in the Dockerfile
# from the pytorch cu121 index so the GPU image does not float on CPU wheels.
audio-separator[gpu]==0.47.0
boto3==1.35.99
botocore==1.35.99
numpy==2.1.3
soundfile==0.12.1
pydub==0.25.1
